# Diagnostics — context

## Invariants

- The module compiles only with the `bench` feature.
- Diagnostics do not bypass normal runtime initialization.
- Production commands and the native generation API do not depend on it.
